import React from "react";
import style from "@/assets/scss/components/header.module.scss";

const Header = () => {
  return (
    <div className={style.header}>
      <div className={style.header__title}>Create Custom Notion Widget</div>
    </div>
  );
};

export default Header;
